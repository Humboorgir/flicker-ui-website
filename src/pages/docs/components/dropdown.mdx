---
title: "Dropdown"
description: "Displays a list of links on a temporary surface that can be opened / closed."
componentName: "dropdown"
preview: "dropdown-default"
---

import Dropdown from "@/components/ui/dropdown";
import Typography from "@/components/ui/typography";
import PreviewBox from "@/components/docs/previewBox";

## Installation

The following command will add the Dropdown component into your `components/ui` folder

```bash
$ npx flicker-ui@latest add dropdown
```

<br />

### Default

     <PreviewBox className="mb-8" component="dropdown-default"/>
