---
title: "Column"
description: "Flexbox container with flex-direction set to column, used for improved code readability"
preview: "column-horizontally-centered"
---

import PreviewBox from "@/components/docs/previewBox";
import Column from "@/components/ui/column";

## Installation

The following command will add the Column component into your `components/ui` folder

```bash
$ npx flicker-ui@latest add column
```

<br />

## Usage

You can specify `align-items` with `items`, and `justify-content` with `justify`

```js /items="center"/ /justify="center"/
<Column items="center" justify="center">
  <div className="bg-red-500 h-16 w-16 rounded mr-3"></div>
  <div className="bg-red-500 h-16 w-16 rounded mr-3"></div>
  <div className="bg-red-500 h-16 w-16 rounded"></div>
</Column>
```

<br />
<br />

### Centered column

<PreviewBox className="mb-12" component="column-default"/>

### Vertically centered

<PreviewBox className="mb-12" component="column-vertically-centered"/>

### Horizontally centered

<PreviewBox component="column-horizontally-centered"/>