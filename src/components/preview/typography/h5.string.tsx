export default // @prettier-ignore
`import Typography from '@/components/ui/typography';

const Preview = () => {
  return (
    <Typography variant="h5">Hello there my friend!</Typography>
  )
}

export default Preview`;
