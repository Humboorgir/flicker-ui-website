export default // @prettier-ignore
`import Button from "@/components/ui/button";

const Preview = () => {
  return <Button variant="secondary">Button component</Button>;
};

export default Preview;`;
